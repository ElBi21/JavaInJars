package Lesson_11;

// We call functional interface an interface with only one abstract method. Functional Interfaces are important for
// making lambda expressions, since lambda expressions are just a way to implement a functional interface's method
// without creating an anonymous class
public interface Functio {
    public String addString(String aString);
}
